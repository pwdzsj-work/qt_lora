# 同一 WiFi 下搜索 ESP32 设备

上位机现已支持通过局域网搜索 ESP32 继电器终端。LoRa 串口功能仍然保留，两种通信方式可以共存。

## 设备端准备

ESP32 现有固件已经提供 WiFi 配网页面和 `/api/status` 状态接口，不需要修改固件协议。

1. 长按终端 `SW3` 约 3 秒，进入 WiFi 配网模式。
2. 手机或电脑连接终端创建的 WiFi 热点。
3. 打开终端的配网页面，填写现场路由器的 WiFi 名称和密码。
4. 配网完成后，让运行上位机的电脑也连接到同一个路由器。

电脑和终端必须处于同一 IPv4 子网。例如电脑是 `192.168.1.20`，终端应为 `192.168.1.x`。

## 上位机操作

1. 运行 `build-qt6-debug\debug\FormalClientTools_V2.1.1.exe`。
2. 打开“配置”→“关于软件”。
3. 在“WiFi设备”一栏点击“搜索同一WiFi设备”。
4. 搜索到的设备会自动加入首页，地址显示为 `WIFI:设备IP`。

搜索完成后，上位机每 2 秒读取一次：

- 继电器开关状态
- NQ1～NQ4 输入状态
- 电压输入
- 4-20mA 电流输入
- 模拟量通道模式

设备连续 3 次无响应后会标记为离线。

## 搜索协议

上位机会对本机活动网卡所在的 `/24` 子网发送：

```http
GET http://设备IP/api/status
```

只有返回有效继电器状态 JSON 的设备才会被识别为终端，普通局域网设备不会加入设备列表。

如果搜索不到设备，请依次确认：

- ESP32 已完成配网并成功连接路由器。
- 电脑与 ESP32 在同一个 WiFi 和同一网段。
- Windows 防火墙或路由器没有开启客户端隔离。
- 浏览器能够打开 `http://设备IP/api/status`。
